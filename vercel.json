{
  "version": 2,
  "buildCommand": "cd ui && npm install && npm run build",
  "outputDirectory": "ui/.next",
  "framework": "nextjs",
  "rewrites": [
    {
      "source": "/api/:match*",
      "destination": "https://uslexragsystem-production.up.railway.app/:match*"
    }
  ]
}
